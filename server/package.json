{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "rimraf build/ && tsc-watch -p ./tsconfig.json --onSuccess \"nodemon ./build/Server.js --watch build\"",
    "build": "rimraf build/ && tsc -p ./tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^12.0.12",
    "axios": "^0.19.0",
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "gmail-send": "^1.8.10",
    "nodemon": "^1.19.1",
    "readline-sync": "^1.4.10",
    "rimraf": "^2.6.3",
    "tsc-watch": "^2.2.1",
    "typescript": "^3.5.2",
    "winston": "^3.2.1"
  }
}
